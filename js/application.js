$('document').ready(function () {

});

var numRange = _.range(0,100);

var problem = function () {
    var a = _.sample(numRange, 1);
    var b = _.sample(numRange, 1);
    var operator = "+";
    var probProcess = a 

    while (a )

};


console.log(numRange);